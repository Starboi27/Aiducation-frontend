# 📄 AI 분석 콜백 연동 및 비동기 처리 설계서 (v1.1)

## 1. 개요
현재 `UploadPage`에서 발생하는 `options.subjectId가 필요합니다` 에러를 해결하고, 백엔드의 **AI Callback Controller** 아키텍처에 맞게 프론트엔드-백엔드-AI 서버 간의 비동기 데이터 흐름을 완성한다.

---

## 2. 현재 문제 분석
1.  **에러 원인:** `aiService.js`의 실제 API 로직은 `subjectId`를 필수값으로 요구하나, 호출부인 `FileUploader.jsx`에서 이를 생성하거나 전달하지 않음.
2.  **흐름 불일치:** 백엔드는 AI 서버와의 비동기 콜백(Callback)을 통해 데이터를 업데이트하도록 설계되었으나, 프론트엔드는 파일 업로드 즉시 결과를 기대하는 동기적 방식으로 구현됨.

---

## 3. 개선된 아키텍처 및 데이터 흐름

### [Step 1: 과목 생성 및 ID 확보]
1.  **Client (UI):** 사용자가 '분석 시작' 버튼 클릭.
2.  **Client (Service):** `subjectService.createSubject` 호출.
3.  **Server:** DB에 새로운 과목 레코드 생성 후 `subjectId` 반환.

### [Step 2: 파일 업로드 및 AI 파이프라인 트리거]
4.  **Client (Service):** `aiService.analyzeDocument(file, { subjectId })` 호출.
5.  **Server:** 파일을 저장하고 AI 서버에 분석 요청을 보낸 뒤 즉시 HTTP 200(성공) 응답.
    *   *참고: 이때 백엔드는 AI 서버에 자신의 콜백 URL을 전달함.*

### [Step 3: 비동기 대기 및 콜백 (Background)]
6.  **AI Server:** 문서 분석 후 백엔드의 `/api/v1/internal/callback/concepts-extracted` 호출.
7.  **Server (Callback):** 수신된 개념(Concepts) 데이터를 해당 `subjectId`에 맞게 DB 업데이트.

### [Step 4: 결과 획득 (Polling)]
8.  **Client (UI):** '분석 중...' 상태를 유지하며 2~3초 간격으로 `GET /api/v1/subjects/{id}/concepts` 호출.
9.  **Server:** DB에 데이터가 존재하면 반환, 없으면 빈 배열 반환.
10. **Client:** 데이터 수신 시 분석 완료 화면으로 이동.

---

## 4. 상세 변경 사항

### 4.1 Frontend - `aiService.js`
*   **`realAnalyzeDocument` 로직 변경:**
    *   단순 API 호출에서 **폴링(Polling) 루프** 포함 로직으로 변경.
    *   `onProgress` 콜백을 통해 UI에 실시간 진행률(예: 40% -> 60% -> 80%) 전달.

### 4.2 Frontend - `FileUploader.jsx`
*   **`analyzeFiles` 핸들러 수정:**
    *   `aiService.analyzeDocument` 호출 전 `subjectService.createSubject` 선행 호출.
    *   에러 핸들링 강화 (과목 생성 실패 시 분석 중단).

### 4.3 Backend (점검 사항)
*   **파일 업로드 API:** AI 서버로 분석 요청을 보낸 후 결과를 기다리지 않고 즉시 응답하는지 확인.
*   **콜백 컨트롤러:** AI 서버로부터 받은 JSON 데이터를 `Concept` 엔티티로 변환하여 DB에 정확히 `save` 하는지 확인.

---

## 5. API 명세 (재확인)

| 기능 | Method | Endpoint | 설명 |
| :--- | :--- | :--- | :--- |
| **과목 생성** | `POST` | `/api/v1/subjects` | 분석 전 공간 확보 |
| **파일 업로드** | `POST` | `/api/v1/subjects/{id}/files` | AI 분석 트리거 |
| **결과 조회** | `GET` | `/api/v1/subjects/{id}/concepts` | 콜백 완료 여부 확인용 폴링 |
| **AI 콜백** | `POST` | `/api/v1/internal/callback/concepts-extracted` | AI 서버 -> 백엔드 데이터 전송 |

---

## 6. 기대 효과
*   **에러 해결:** `subjectId` 강제 체크 로직에 부합하는 데이터 흐름 완성.
*   **사용자 경험 개선:** 실제 AI 분석 시간 동안 사용자에게 "분석 중" 상태 시각화 제공.
*   **시스템 안정성:** 비동기 콜백 구조를 통한 타임아웃 방지 및 데이터 정합성 확보.
